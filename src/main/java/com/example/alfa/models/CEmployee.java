package com.example.alfa.models;

import java.util.Comparator;

public class CEmployee {
    String name;
    boolean isActive;
    String address_city;
    String address_number;
    String address_street;
    String address_country;
}
/*

public class SortbyRoll implements Comparator<CEmployee>{

    // Method
    // Sorting in ascending order of roll number
    public int compare(CEmployee a, CEmployee b)
    {

        return a.rollno - b.rollno;
    }

}

public class SortbyEmployeeNumber implements Comparator<CEmployee>{

    // Method
    // Sorting in ascending order of roll number
    public int compare(CEmployee a, CEmployee b)
    {
        return a. - b.rollno;
    }
}

public class Sortbyname implements Comparator<CEmployee>{

    // Method
    // Sorting in ascending order of roll number
    public int compare(CEmployee a, CEmployee b)
    {

        return a.name.compareTo(b.name);
    }

}

public class SortbyYearlySalary implements Comparator<CEmployee>{

    // Method
    // Sorting in ascending order of roll number
    public int compare(CEmployee a, CEmployee b)
    {

        return a.rollno - b.rollno;
    }

}
*/






